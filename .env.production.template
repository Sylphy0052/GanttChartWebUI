# GanttChartWebUI - Production Environment Variables Template
# Copy this file to .env.production and update the values for your production environment
# IMPORTANT: Never commit .env.production to version control

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
POSTGRES_DB=gantt_chart
POSTGRES_USER=gantt_user
# Generate a strong password: openssl rand -base64 32
POSTGRES_PASSWORD=CHANGE_ME_STRONG_DATABASE_PASSWORD

# =============================================================================
# REDIS CONFIGURATION
# =============================================================================
# Generate a strong password: openssl rand -base64 32
REDIS_PASSWORD=CHANGE_ME_STRONG_REDIS_PASSWORD

# =============================================================================
# JWT CONFIGURATION
# =============================================================================
# Generate a strong secret: openssl rand -base64 64
JWT_SECRET=CHANGE_ME_STRONG_JWT_SECRET_KEY_64_CHARACTERS_MINIMUM
JWT_AUDIENCE=gantt-chart-web-ui
JWT_ISSUER=gantt-chart-api

# =============================================================================
# APPLICATION URLs
# =============================================================================
# Update these URLs to match your production domain
CORS_ORIGIN=https://your-domain.com
NEXT_PUBLIC_API_URL=https://your-domain.com/api
NEXT_PUBLIC_API_BASE=https://your-domain.com/api/v1

# =============================================================================
# LOGGING CONFIGURATION
# =============================================================================
LOG_LEVEL=info

# =============================================================================
# DATA STORAGE PATH
# =============================================================================
# Path for persistent data volumes (ensure directory exists and has proper permissions)
DATA_PATH=./data

# =============================================================================
# SECURITY NOTES
# =============================================================================
# 1. Replace all "CHANGE_ME_*" values with strong, unique values
# 2. Use different secrets for each environment (dev, staging, production)
# 3. Store secrets securely using a secret management system in production
# 4. Regularly rotate secrets according to your security policy
# 5. Ensure file permissions are restricted: chmod 600 .env.production